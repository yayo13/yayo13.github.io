<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.9.0">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2021-04-28T13:52:08+08:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">zizi7</title><subtitle></subtitle><author><name>zizi7</name></author><entry><title type="html">Gabor滤波器</title><link href="http://localhost:4000/2016/11/06/Gabor%E6%BB%A4%E6%B3%A2%E5%99%A8/" rel="alternate" type="text/html" title="Gabor滤波器" /><published>2016-11-06T00:00:00+08:00</published><updated>2016-11-06T00:00:00+08:00</updated><id>http://localhost:4000/2016/11/06/Gabor%E6%BB%A4%E6%B3%A2%E5%99%A8</id><content type="html" xml:base="http://localhost:4000/2016/11/06/Gabor%E6%BB%A4%E6%B3%A2%E5%99%A8/">&lt;p&gt;Gabor是一个用于边缘提取的线性滤波器，其频率和方向表达与人类视觉系统类似，能够提供良好的方向选择和尺度选择特性，而且对于光照变化不敏感，因此十分适合纹理分析。&lt;/p&gt;

&lt;p&gt;图1是&lt;a href=&quot;http://www.cnblogs.com/emouse/p/3611256.html&quot;&gt;Gabor滤波器和脊椎动物视觉皮层感受野响应的比较&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;　　　　　　&lt;img src=&quot;/assets/images/articles/20161106_01/20161104173725944.png&quot; alt=&quot;Gabor与脊椎动物视觉响应的比较&quot; /&gt;
　　　　　　　　　　　　　　图1. Gabor与脊椎动物视觉皮层感受野响应的比较&lt;/p&gt;

&lt;p&gt;图1中第一行是脊椎动物的视觉响应，第二行是Gabor滤波器的响应，可以看到，二者相差极小。
基于以上特性，Gabor滤波器被广泛应用于人脸识别的预处理。&lt;/p&gt;

&lt;hr /&gt;

&lt;h1 id=&quot;gabor理论及公式&quot;&gt;Gabor理论及公式&lt;/h1&gt;
&lt;p&gt;我们知道，傅里叶变换可以将信号从时域转换到频域，但无法获得频谱中不同频率之间的先后关系。
然而实际应用中我们更多的关心信号局部范围内的的特性，&lt;strong&gt;Gabor和小波变换突破了傅里叶变换的局限性&lt;/strong&gt;。&lt;/p&gt;

&lt;p&gt;Gabor变换是D.Gabor于1946年提出的，为了提取傅里叶变换的局部信息，引入了时间局部化的窗函数（把信号划分成许多小的时间间隔，用傅里叶变换分析每一个间隔）。因此&lt;strong&gt;Gabor变换又称为窗口傅里叶变换（短时傅里叶变换）&lt;/strong&gt;。&lt;/p&gt;

&lt;h2 id=&quot;二维gabor滤波器&quot;&gt;二维Gabor滤波器&lt;/h2&gt;
&lt;p&gt;在空间域，一个二维的Gabor滤波器是一个正弦平面波和高斯核函数的乘积。前者是调谐函数，后者是窗口函数。&lt;/p&gt;

\[g(x,y;\lambda,\theta,\psi,\sigma,\gamma)=e^{-\frac{x^{'^2}+\gamma^2y^{'^2}}{2\sigma^2}}e^{i(2\pi\frac{x^{'}}{\lambda}+\psi)}\]

&lt;p&gt;可以分为实部与虚部的形式&lt;/p&gt;

\[\begin{cases}g_{real}(x,y;\lambda,\theta,\psi,\sigma,\gamma)=e^{-\frac{x^{'^2}+\gamma^2y^{'^2}}{2\sigma^2}}cos(2\pi\frac{x^{'}}{\lambda}+\psi)\\g_{imag}(x,y;\lambda,\theta,\psi,\sigma,\gamma)=e^{-\frac{x^{'^2}+\gamma^2y^{'^2}}{2\sigma^2}}sin(2\pi\frac{x^{'}}{\lambda}+\psi)\end{cases}\]

&lt;p&gt;其中&lt;/p&gt;

\[\begin{cases}x^{'}=xcos\theta+ysin\theta\\y^{'}=-xsin\theta+ycos\theta\end{cases}\]

&lt;p&gt;　　　　　　　　　　　　&lt;strong&gt;表1. 二维Gabor滤波器参数解释&lt;/strong&gt;&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th style=&quot;text-align: center&quot;&gt;参数&lt;/th&gt;
      &lt;th style=&quot;text-align: center&quot;&gt;物理意义&lt;/th&gt;
      &lt;th style=&quot;text-align: center&quot;&gt;描述&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;$\lambda$&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;波长&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;直接影响滤波器的滤波尺度，通常大于等于2&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;$\theta$&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;方向&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;滤波器的方向&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;$\psi$&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;相位偏移&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;调谐函数的相位偏移，取值-180到180&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;$\gamma$&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;空间纵横比&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;决定滤波器的形状，取1时为圆形，通常取0.5&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;$\sigma$&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;带宽&lt;/td&gt;
      &lt;td style=&quot;text-align: center&quot;&gt;高斯滤波器的方差，通常取2$\pi$&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;hr /&gt;

&lt;h1 id=&quot;实验&quot;&gt;实验&lt;/h1&gt;
&lt;p&gt;本节参考自&lt;a href=&quot;http://blog.csdn.net/xiaowei_cqu/article/details/24745945&quot;&gt;【图像处理】Gabor滤波器&lt;/a&gt;、&lt;a href=&quot;http://blog.csdn.net/jinshengtao/article/details/17797641&quot;&gt;Gabor滤波器学习&lt;/a&gt;、&lt;a href=&quot;http://blog.csdn.net/guoming0000/article/details/7839917&quot;&gt;Gabor的OpenCV代码&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;　　　　　　　　　　　　　　　&lt;img src=&quot;/assets/images/articles/20161106_01/20161106160742196.png&quot; alt=&quot;不同参数的Gabor滤波器&quot; /&gt;
　　　　　　　　　　　　　　图2. 取不同参数的Gabor滤波器&lt;/p&gt;

&lt;p&gt;图2对比了不同参数的Gabor滤波器，从第1到第5行$\lambda$取3,6,9,12,15，从第1到第8列$\theta$取0,$\frac{\pi}{8}$,$\frac{\pi}{4}$,$\frac{3\pi}{8}$,$\frac{\pi}{2}$,$\frac{5\pi}{8}$,$\frac{3\pi}{4}$,$\frac{7\pi}{8}$，其他参数保持不变：$\psi=0, \sigma=2\pi, \gamma=0.5$。&lt;/p&gt;

&lt;p&gt;图4使用图2的滤波器对图3中两幅人脸做卷积
　　　　　　　　　　　　　　　　　　　　　　　　&lt;img src=&quot;/assets/images/articles/20161106_01/20161106162324717.png&quot; alt=&quot;FERET人脸&quot; /&gt;
　　　　　　　　　　　　　　图3. FERET中同一人在不同光照不同角度的人脸&lt;/p&gt;

&lt;p&gt;　　　　　　　　　　　　&lt;img src=&quot;/assets/images/articles/20161106_01/20161106162709863.png&quot; alt=&quot;Gabor滤波结果&quot; /&gt;
　　　　　　　　　　　　　　图4.1. 使用图2滤波器对图3左卷积结果&lt;/p&gt;

&lt;p&gt;　　　　　　　　　　　　&lt;img src=&quot;/assets/images/articles/20161106_01/20161106162835365.png&quot; alt=&quot;Gabor滤波结果&quot; /&gt;
　　　　　　　　　　　　　　图4.2. 使用图2滤波器对图3右卷积结果&lt;/p&gt;

&lt;p&gt;可以看到，不同方向不同尺度的Gabor滤波器可以提取人脸中不同特征
此外还可以看到，Gabor滤波结果对不同光照也能保持较稳定结果&lt;/p&gt;

&lt;hr /&gt;
&lt;h2 id=&quot;代码&quot;&gt;代码&lt;/h2&gt;

&lt;div class=&quot;language-plaintext highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;// gaborfilter.cpp
#include &quot;gaborfilter.h&quot;

int CGabor::getFilterSize(float theta, float sigma, float gamma)
{
    float sigma_x = sigma*sigma;
    float sigma_y = sigma_x/(gamma*gamma);
    float sqrt_sigma_y = sqrt(sigma_y);
    float c_theta = cos(theta);
    float s_theta = sin(theta);

    // calculate filter size (3sigma)
    int nstds = 3;
    int xmax = max(abs(nstds*sigma*c_theta), abs(nstds*sqrt_sigma_y*s_theta));
    int ymax = max(abs(nstds*sigma*s_theta), abs(nstds*sqrt_sigma_y*c_theta));
    int half_filter_size = xmax&amp;gt;ymax?xmax:ymax;
    int filter_size = 2*half_filter_size+1;

    return filter_size;
}

void CGabor::getGaborFilter(float lambda, float theta, float fhi, float sigma, float gamma, 
    Mat&amp;amp; realGabor, Mat&amp;amp; imagGabor, Mat&amp;amp; mag)
{
    if(abs(lambda-0.0f) &amp;lt; 1e-6)
        lambda = 1.0f;

    float sigma_x = sigma*sigma;
    float sigma_y = sigma_x/(gamma*gamma);
    float sqrt_sigma_y = sqrt(sigma_y);
    float c_theta = cos(theta);
    float s_theta = sin(theta);

    int filter_size = getFilterSize(theta, sigma, gamma);
    int half_filter_size = (filter_size-1)/2;

    realGabor = Mat(filter_size, filter_size, CV_32F);
    imagGabor = Mat(filter_size, filter_size, CV_32F);
    mag       = Mat(filter_size, filter_size, CV_32F);

    for(int i=0; i&amp;lt;filter_size; i++)
    {
        float* p_real = realGabor.ptr&amp;lt;float&amp;gt;(i);
        float* p_imag = imagGabor.ptr&amp;lt;float&amp;gt;(i);
        float* p_mag  = mag.ptr&amp;lt;float&amp;gt;(i);
        int y = i - half_filter_size;

        for(int j=0; j&amp;lt;filter_size; j++)
        {
            int x = j - half_filter_size;
            float x_theta = x*c_theta + y*s_theta;
            float y_theta = y*c_theta - x*s_theta;

            float value_ = exp(-0.5*(x_theta*x_theta/sigma_x + y_theta*y_theta/sigma_y));
            p_real[j] = value_ * cos(2*CV_PI*x_theta/lambda + fhi);
            p_imag[j] = value_ * sin(2*CV_PI*x_theta/lambda + fhi);
            p_mag[j]  = sqrt(p_real[j]*p_real[j] + p_imag[j]*p_imag[j]);
        }
    }
}

Mat CGabor::crossGaborFilter(Mat gaborFilter, Mat image)
{
    CV_Assert(!gaborFilter.empty() &amp;amp;&amp;amp; !image.empty());

    int half_filter_size = (max(gaborFilter.rows, gaborFilter.cols)-1)/2;
    Mat filtered_img(image.rows, image.cols, CV_32F);

    for(int i=0; i&amp;lt;image.rows; i++)
    {
        float* p_fil = filtered_img.ptr&amp;lt;float&amp;gt;(i);
        for(int j=0; j&amp;lt;image.cols; j++)
        {
            float sum_value = 0.0f;
            for(int m=0; m&amp;lt;gaborFilter.rows; m++)
            {
                float* p_gab = gaborFilter.ptr&amp;lt;float&amp;gt;(m);
                
                int img_i = i + m - half_filter_size;
                img_i = img_i&amp;lt;0 ? 0 : img_i;
                img_i = img_i&amp;gt;=image.rows ? (image.rows-1) : img_i;
                uchar* p_img = image.ptr&amp;lt;uchar&amp;gt;(img_i);
                for(int n=0; n&amp;lt;gaborFilter.cols; n++)
                {
                    int img_j = j + n - half_filter_size;
                    img_j = img_j&amp;lt;0 ? 0 : img_j;
                    img_j = img_j&amp;gt;=image.cols ? (image.cols-1) : img_j;

                    sum_value += ((float)p_img[img_j] * p_gab[n]);
                }
            }
            p_fil[j] = sum_value;
        }
    }
    return filtered_img;
}

Mat CGabor::normalizer(Mat src)
{
    CV_Assert(!src.empty());

    float min_ = FLT_MAX;
    float max_ = FLT_MIN;
    for(int i=0; i&amp;lt;src.rows; i++)
    {
        float* p_src = src.ptr&amp;lt;float&amp;gt;(i);
        for(int j=0; j&amp;lt;src.cols; j++)
        {
            if(p_src[j] &amp;gt; max_)
                max_ = p_src[j];
            if(p_src[j] &amp;lt; min_)
                min_ = p_src[j];
        }
    }

    Mat src_show(src.size(), CV_8UC1);
    float scale = max_ - min_;
    for(int i=0; i&amp;lt;src.rows; i++)
    {
        float* p_src = src.ptr&amp;lt;float&amp;gt;(i);
        uchar* p_src_show = src_show.ptr&amp;lt;uchar&amp;gt;(i);
        for(int j=0; j&amp;lt;src.cols; j++)
        {
            if(scale &amp;gt; 0.01)
                p_src_show[j] = (uchar)((p_src[j]-min_)*255/scale);
            else
                p_src_show[j] = 255;
        }
    }
    return src_show;
}
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;div class=&quot;language-plaintext highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;\\ main.cpp
#include &quot;stdafx.h&quot;
#include &quot;gaborfilter.h&quot;


void main()
{
    Mat img[7];
    string path = &quot;C:\\Users\\sq\\Desktop\\humanface\\FaceDataSet\\FERET_80_80\\FERET-001\\&quot;;
    for(int i=0; i&amp;lt;7; i++)
    {
        char num[7];
        sprintf(num, &quot;0%d.tif&quot;, i+1);
        string wholepath = num;
        wholepath = path + wholepath;

        Mat tmp = imread(wholepath, 0);
        resize(tmp, img[i], Size(200, 200)); // 为了确保滤波器尺寸小于图片大小
    }

    // calc gabor
    CGabor m_gabor;
    float lambda = 5;
    float theta = 0;
    float fhi = 0;
    float sigma = 2*CV_PI;
    float gamma = 0.5;

    Mat realGabor[40], imagGabor, magGabor;
    
    int size_ = INT_MAX;
    for(int i=0; i&amp;lt;8; i++)
    {
        theta = i*CV_PI/8;
        int _size_ = m_gabor.getFilterSize(theta, sigma, gamma);

        if(_size_ &amp;lt; size_)
            size_ = _size_;
    }
    Mat showit((size_+3)*5-3, (size_+3)*8-3, CV_8UC1, Scalar::all(255));

    for(int i=0; i&amp;lt;8; i++)
    {
        theta = i*CV_PI/8;
        for(int j=0; j&amp;lt;5; j++)
        {
            lambda = (j+1)*3;
            m_gabor.getGaborFilter(lambda, theta, fhi, sigma, gamma, realGabor[j*8+i], imagGabor, magGabor);
            Mat saveimg = m_gabor.normalizer(realGabor[j*8+i]);

            Mat saveimg_roi(saveimg, Rect(saveimg.rows/2-size_/2, saveimg.rows/2-size_/2, size_, size_));
            int start_row = j*(size_+3);
            int start_col = i*(size_+3);
            for(int m=0; m&amp;lt;size_; m++)
            {
                uchar* p_img = saveimg_roi.ptr&amp;lt;uchar&amp;gt;(m);
                uchar* p_show = showit.ptr&amp;lt;uchar&amp;gt;(start_row + m);
                for(int n=0; n&amp;lt;size_; n++)
                   p_show[start_col + n] = p_img[n];
            }
        }
    }
    
    imshow(&quot;gabor&quot;, showit);
    imwrite(&quot;gabor.jpg&quot;, showit);
    waitKey(1);

    // filter face
    for(int i=0; i&amp;lt;7; i++)
    {
        Mat face((img[0].rows+3)*5-3, (img[0].cols+3)*8-3, CV_8UC1, Scalar::all(255));
        for(int m=0; m&amp;lt;5; m++)
        {
            int start_row = m*(img[0].rows+3);
            for(int n=0; n&amp;lt;8; n++)
            {
                Mat filted = m_gabor.crossGaborFilter(realGabor[m*8+n], img[i]);
                Mat filted_norm = m_gabor.normalizer(filted);

                int start_col = n*(img[0].cols+3);
                for(int ro=0; ro&amp;lt;img[0].rows; ro++)
                {
                    uchar* p_filted = filted_norm.ptr&amp;lt;uchar&amp;gt;(ro);
                    uchar* p_face = face.ptr&amp;lt;uchar&amp;gt;(start_row + ro);
                    for(int co=0; co&amp;lt;img[0].cols; co++)
                        p_face[start_col + co] = p_filted[co];
                }
            }
        }

        char num[7];
        sprintf(num, &quot;_%d.jpg&quot;, i);
        string filename = num;
        filename = &quot;face&quot;+filename;
        imwrite(filename, face);
        imshow(filename, face);
        waitKey(1);
    }
    waitKey(0);
}
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;hr /&gt;</content><author><name>zizi7</name></author><category term="图像处理" /><summary type="html">Gabor是一个用于边缘提取的线性滤波器，其频率和方向表达与人类视觉系统类似，能够提供良好的方向选择和尺度选择特性，而且对于光照变化不敏感，因此十分适合纹理分析。</summary></entry><entry><title type="html">《图像局部不变性特征与描述》阅读笔记（1）– 背景</title><link href="http://localhost:4000/2015/11/24/%E5%9B%BE%E5%83%8F%E5%B1%80%E9%83%A8%E4%B8%8D%E5%8F%98%E6%80%A7%E7%89%B9%E5%BE%81%E4%B8%8E%E6%8F%8F%E8%BF%B0-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-1-%E8%83%8C%E6%99%AF/" rel="alternate" type="text/html" title="《图像局部不变性特征与描述》阅读笔记（1）– 背景" /><published>2015-11-24T00:00:00+08:00</published><updated>2015-11-24T00:00:00+08:00</updated><id>http://localhost:4000/2015/11/24/%E3%80%8A%E5%9B%BE%E5%83%8F%E5%B1%80%E9%83%A8%E4%B8%8D%E5%8F%98%E6%80%A7%E7%89%B9%E5%BE%81%E4%B8%8E%E6%8F%8F%E8%BF%B0%E3%80%8B%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0(1)--%20%E8%83%8C%E6%99%AF</id><content type="html" xml:base="http://localhost:4000/2015/11/24/%E5%9B%BE%E5%83%8F%E5%B1%80%E9%83%A8%E4%B8%8D%E5%8F%98%E6%80%A7%E7%89%B9%E5%BE%81%E4%B8%8E%E6%8F%8F%E8%BF%B0-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0-1-%E8%83%8C%E6%99%AF/">&lt;h1 id=&quot;1-局部特征概念及性质&quot;&gt;1. 局部特征概念及性质&lt;/h1&gt;
&lt;p&gt;局部特征是从图像局部区域中抽取的特征，可以提供一种具有统计意义的图像内容表述；全局特征（比如颜色直方图）可以描述图像的内容，具有很好的检索性能，但受图像遮挡的影响较大。&lt;/p&gt;

&lt;p&gt;局部特征最重要是需要满足&lt;strong&gt;可重复性&lt;/strong&gt;：同一场景在不同视角（几何变形）下，特征能保持自身的稳定（也就是不变性：令f为特征提取函数，t为某种几何变换，对于图像x，应有 f(t(x))=f(x)）。此外，定义的局部特征应能在图像上检测到&lt;strong&gt;足够多的数量&lt;/strong&gt;，然后通过阈值进行筛选。&lt;/p&gt;

&lt;hr /&gt;
&lt;h1 id=&quot;2-局部特征的发展&quot;&gt;2. 局部特征的发展&lt;/h1&gt;
&lt;p&gt;&lt;img src=&quot;/assets/images/articles/20151124_01/20151125103827671.png&quot; alt=&quot;在这里插入图片描述&quot; /&gt;
图2-1. 局部特征发展历程&lt;/p&gt;

&lt;p&gt;&lt;mark&gt;Moravec角点&lt;/mark&gt;考察当前点与其4-邻域（8-邻域）像素的相似性，只有当4（8）个方向的相似度都小于给定阈值才认为当前点为角点&lt;/p&gt;

\[E(u,v)=\sum_{x,y}w(x,y)[I(x+u,y+v)-I(x,y)]^2\]

&lt;p&gt;&lt;strong&gt;算法步骤&lt;/strong&gt;（参考http://blog.csdn.net/lu597203933/article/details/15027711）：&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;对每个点，计算E(u,v)。(u,v)取值为(1,0), (1,1), (0,1), (-1,1)，代表4个方向&lt;/li&gt;
  &lt;li&gt;选取E(u,v)最小的那个值（方向）作为备选（存在一个与原始图相同大小的矩阵M里）&lt;/li&gt;
  &lt;li&gt;对M中的每个点，计算w窗口内取最大的那个值，如果该值大于阈值，则认为原始图中该坐标的点为特征点；之后M中的这个点平移w窗口大小的位置（与上个窗口不重叠），重复3（3中之所以在窗口内选最大值其实是起到了一定的筛选作用）&lt;/li&gt;
&lt;/ol&gt;

&lt;p&gt;可以看到，因为只计算了4个方向（最多8个），因此当同一场景旋转一定角度后，之前能检测到的Moravec角点可能就检测不到了（因为不在这几个方向中）。也就是说，&lt;strong&gt;Moravec角点不具备旋转不变性&lt;/strong&gt;，此外，其&lt;strong&gt;对噪声也很敏感&lt;/strong&gt;。因此现在已经被淘汰。&lt;/p&gt;

&lt;p&gt;&lt;mark&gt;Harris角点&lt;/mark&gt;是Moravec角点的改进算法。针对Moravec角点对方向依赖性强，使用微分算子替代了窗口的移动；使用高斯窗口（模拟圆形窗口）替代Moravec的方形窗口，降低对噪声的敏感度；对E（见Moravec公式）变形为二次型，以区分边缘和角点。相对Moravec角点，&lt;strong&gt;Harris角点具备旋转不变性，具备平移不变性，但不具备空间尺度不变性&lt;/strong&gt;（缩放）&lt;/p&gt;

&lt;p&gt;&lt;mark&gt;Harris-Laplacian角点&lt;/mark&gt;结合Harris和高斯尺度空间，使角点&lt;strong&gt;增加了空间尺度不变性&lt;/strong&gt;。&lt;/p&gt;

&lt;p&gt;&lt;mark&gt;Harris-Affine角点&lt;/mark&gt;具有&lt;strong&gt;仿射不变性&lt;/strong&gt;（仿射变换就是将某向量进行旋转、放缩和平移：x-&amp;gt;Ax+b）&lt;/p&gt;

&lt;p&gt;&lt;mark&gt;SIFT&lt;/mark&gt;（Scale Invariant Feature Transform）特征利用金字塔和高斯滤波差分来快速求解高斯拉普拉斯空间中的极值点，&lt;strong&gt;能快速提取具有仿射不变性的特征点&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;&lt;mark&gt;SURF&lt;/mark&gt;（Speeded Up Robust Feature）特征是SIFT的改进，通过积分图像和Harr小波&lt;strong&gt;进一步提高了检测速度&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;&lt;mark&gt;MSERs&lt;/mark&gt;（Maximally Stable Extrernal Regions）特征借用分水岭的思路检测灰度最稳定的区域，然后对其进行旋转和尺度的归一化，&lt;strong&gt;具有严格的仿射不变性&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;从局部特征点的发展历程可以看到，局部特征的关注点集中在可重复性（各种不变性）。&lt;/p&gt;</content><author><name>zizi7</name></author><category term="图像处理" /><summary type="html">1. 局部特征概念及性质 局部特征是从图像局部区域中抽取的特征，可以提供一种具有统计意义的图像内容表述；全局特征（比如颜色直方图）可以描述图像的内容，具有很好的检索性能，但受图像遮挡的影响较大。</summary></entry></feed>